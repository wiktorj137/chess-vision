## What this changes

<!-- One idea per PR. -->

## Checklist

- [ ] `npm test` is green
- [ ] new logic in `src/attacks.js` has tests — one position where it fires,
      one where it must not
- [ ] no new dependencies
- [ ] player-facing text added to **both** language tables in `src/content.js`
- [ ] if this adds a pattern: it has a colour, a shape, and a legend row

## For chess changes

Paste a FEN a reviewer can check against.
